---
phase: quick-260916-sz4
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [ISSUE-117]
issue: 117
branch: main
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All three `.planning/` statements of GATE-04 assert the same rule — that the unit of review, not the PR object, is what may not widen more than one category. The contradiction issue #117 names no longer exists between STATE.md and REQUIREMENTS.md."
    - "GATE-04 in `.planning/REQUIREMENTS.md` is a checked box. It was the only unchecked gate in that section; the other four (GATE-01, GATE-02, GATE-03, GATE-05) are already checked and must stay checked."
    - "The amended REQUIREMENTS.md bullet records WHY the gate moved — that the previous wording was never once satisfiable, with both counter-examples (CAT-01 direct to `main`, CAT-02 across two PRs) named in the text."
    - "Not one byte outside the three named `.planning/` files changes: no Rust source, no `patterns/` YAML, no test, no corpus file, no README. `cargo test` is not a gate for this change because nothing it could observe has moved."
    - "The wording is the developer's verbatim text, applied character-for-character. No paraphrase, no re-wrapping to a different column, no re-worded rationale."
    - "`main` remains strictly linear — this change adds exactly one non-merge commit."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "`.planning/REQUIREMENTS.md` GATE-04 bullet -> `.planning/STATE.md` standing-gates table row. These two are the exact pair issue #117 was filed about; if they disagree after this change, the task has failed regardless of what else passed."
    - "`.planning/ROADMAP.md` gates table -> `.planning/STATE.md` gates table. Same five-row table, two copies, historically kept in sync — the ROADMAP copy is the terse one, the STATE copy carries the blast-radius rationale clause."
    - "`.planning/REQUIREMENTS.md:115` coverage counter (`5 feature requirements, 5 mapped. 5 gates apply to every phase. Unmapped: 0`) -> the gates section. Checking a box does not change any count in that line; if the executor edits it, that is a regression, not an update."
---

<objective>
Amend GATE-04 from a PR-shaped rule to a review-unit-shaped rule, in all three `.planning/`
documents that state it, using the developer's verbatim wording.

The gate's substance — one category per unit of review — has held on every phase of this
milestone. Its *wording* has not been satisfiable once: CAT-01 shipped directly to `main` with no
PR object to point at, and CAT-02 shipped across two PRs. That mismatch is why GATE-04 is the only
unchecked gate in REQUIREMENTS.md while STATE.md reports the gate as standing and satisfied — the
contradiction issue #117 was filed for.

Purpose: close #117 by fixing the rule text rather than by arguing about which document was right.
Output: three edited `.planning/` files, one atomic commit, no source or test changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Apply the amended GATE-04 wording to all three .planning documents</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    Read the three target regions before editing — each file has exactly one GATE-04 row or bullet
    to change, and each file has OTHER GATE-04 mentions that must be left alone:

    - `.planning/ROADMAP.md` — the gates table under `## Gates applied to every phase`, around
      line 155. One row.
    - `.planning/STATE.md` — the gates table under
      `## Standing gates — these are what made v0.1.0 trustworthy`, around line 250. One row.
      STATE.md also mentions GATE-04 in narrative prose at roughly lines 41, 46, 303-305 and 379.
      Those are dated session notes recording what was true at the time. **Leave every one of them
      exactly as written** — rewriting history to match a later amendment is precisely the failure
      mode this repo's own records warn about.
    - `.planning/REQUIREMENTS.md` — the GATE-04 bullet under
      `### Gates — non-negotiable, they are what made v0.1.0 trustworthy`, lines 72-77. Six lines:
      the unchecked bullet line plus five continuation lines, ending with the line that suggests
      amending the gate. The whole six-line bullet is replaced.
  </read_first>

  <action>
    Three edits, each a whole-element replacement. The replacement text below is the developer's
    verbatim wording — apply it character-for-character. Do not re-word, do not re-wrap to a
    different column, do not "improve" the rationale, do not add a sixth gate row or a footnote.

    EDIT 1 — `.planning/ROADMAP.md`. Replace the entire existing `| GATE-04 | ... |` table row
    (the whole line, around line 155) with:

    | GATE-04 | No reviewable unit widens more than one category |

    EDIT 2 — `.planning/STATE.md`. Replace the entire existing `| GATE-04 | ... |` table row
    (the whole line, around line 250) with:

    | GATE-04 | No reviewable unit widens more than one category — widening four at once is an unreviewable FP blast radius |

    Keep the em dash and the `FP` abbreviation as given — this row is the terse-plus-rationale
    variant and the ROADMAP row is the terse variant; they are deliberately not identical.

    EDIT 3 — `.planning/REQUIREMENTS.md`. Replace the whole existing GATE-04 bullet — the
    unchecked one, from its `- [` marker on line 72 through line 77 inclusive, including its dated
    Phase 3 status paragraph — with the following six lines exactly:

    - [x] **GATE-04**: No reviewable unit widens more than one category. Widening four categories at
          once produces an unreviewable false-positive blast radius — this is a recorded lesson, not a
          preference. **Amended 2026-09-16 (#117):** the gate previously read "its own PR" and was
          never once satisfiable as written — CAT-01 shipped directly to `main` with no PR object, and
          CAT-02 shipped as PR #120 plus PR #136. The substance it protects, one category per unit of
          review, held in every case. The unit is the PR where one exists and the phase's commit range
          where it does not.

    The checkbox flips from unchecked to `[x]`. The continuation indent is 6 spaces and the wrap is
    ~100 columns, matching the four sibling gate bullets in the same section — the text above is
    already wrapped that way, so transcribe the line breaks as given rather than re-flowing.

    Do NOT touch `.planning/REQUIREMENTS.md:115` (`**Coverage:** 5 feature requirements, 5 mapped.
    5 gates apply to every phase. Unmapped: 0 ✓`). Checking a box changes no count in that line.

    Then stage exactly these three files and commit as one atomic docs commit:

      docs(quick-260916-sz4): amend GATE-04 to a review-unit rule, not a PR rule (#117)

    Reference `#117` in the message body; do NOT use a `Closes`/`Fixes` keyword — Task 2 surfaces a
    fourth, out-of-scope copy of the old wording, and the developer decides whether the issue closes
    with that copy still outstanding. `main` stays strictly linear: one non-merge commit, no rebase,
    no merge.
  </action>

  <verify>
    <automated>bash -c 'set -e
      grep -qF "| GATE-04 | No reviewable unit widens more than one category |" .planning/ROADMAP.md
      grep -qF "| GATE-04 | No reviewable unit widens more than one category — widening four at once is an unreviewable FP blast radius |" .planning/STATE.md
      grep -qF "- [x] **GATE-04**: No reviewable unit widens more than one category. Widening four categories at" .planning/REQUIREMENTS.md
      grep -qF "**Amended 2026-09-16 (#117):**" .planning/REQUIREMENTS.md
      grep -qF "CAT-02 shipped as PR #120 plus PR #136." .planning/REQUIREMENTS.md
      ! grep -qE "^\| GATE-04 \| One category per PR" .planning/ROADMAP.md .planning/STATE.md
      ! grep -qF "Status (Phase 3, 2026-09-02):" .planning/REQUIREMENTS.md
      ! grep -qE "^- \[ \] \*\*GATE-04\*\*" .planning/REQUIREMENTS.md
      grep -qF "**Coverage:** 5 feature requirements, 5 mapped. 5 gates apply to every phase. Unmapped: 0" .planning/REQUIREMENTS.md
      echo VERIFY-OK'</automated>
  </verify>

  <done>
    All three files carry the amended wording verbatim; REQUIREMENTS.md GATE-04 is `[x]` and its
    old dated status paragraph is gone; the REQUIREMENTS.md coverage counter is byte-unchanged; the
    old table-row wording survives in neither gates table; one non-merge commit exists on `main`
    touching exactly these three paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit the change for residue and scope leakage, then record it</name>
  <files>.planning/quick/260916-sz4-amend-gate-04-to-no-reviewable-unit-wide/260916-sz4-SUMMARY.md</files>

  <action>
    Three checks, then write the summary.

    CHECK A — scope. Confirm the commit from Task 1 touched exactly three paths, all under
    `.planning/`. If `git show --stat` lists a single file under `src/`, `tests/`, `patterns/`, or
    the repo README, that is a scope leak: revert it in a follow-up commit and say so in the
    summary. Nothing this change touches can move a detection result, a recall number, or a
    pattern count, so none of those numbers may move either.

    CHECK B — linearity. Confirm `git log --oneline --merges origin/main..HEAD` is empty and the new
    commit has exactly one parent. `main` stays strictly linear is a standing repo rule, not a
    preference.

    CHECK C — residue, expected and deliberate. `.planning/.continue-here.md` (around line 72)
    carries a FOURTH copy of the gates table with the superseded terse wording. It is deliberately
    out of scope: the developer named three locations, issue #117 names only STATE.md and
    REQUIREMENTS.md, and `.continue-here.md` is a regenerated session-continuity scratch document
    rather than a requirement source. Do not edit it in this task. Record it in the summary as a
    named, open residue with a one-line recommendation (fold it in the next time that file is
    regenerated, or a two-line follow-up quick task) so the developer can decide whether #117
    closes now or after that copy is reconciled.

    Also record, without changing them: the historical prose mentions of the superseded wording in
    `.planning/STATE.md` (~lines 41, 46, 379) and across
    `.planning/phases/03-tool-permission-abuse-cat-01-33/`. These are dated records of what the
    gate said at the time and are correct as history.

    Write the SUMMARY to the path in `<files>`, following the GSD summary template. State plainly
    what moved, what deliberately did not, and the #117 close recommendation.
  </action>

  <verify>
    <automated>bash -c 'set -e
      test "$(git show --name-only --format= HEAD | grep -c .)" = "3"
      test "$(git show --name-only --format= HEAD | grep -v "^\.planning/" | grep -c .)" = "0"
      test -z "$(git log --oneline --merges -1 HEAD)"
      test "$(git rev-list --parents -n 1 HEAD | wc -w | tr -d " ")" = "2"
      test -f .planning/quick/260916-sz4-amend-gate-04-to-no-reviewable-unit-wide/260916-sz4-SUMMARY.md
      grep -qF ".continue-here.md" .planning/quick/260916-sz4-amend-gate-04-to-no-reviewable-unit-wide/260916-sz4-SUMMARY.md
      echo AUDIT-OK'</automated>
  </verify>

  <done>
    The commit is proven to touch exactly three `.planning/` files and nothing else; HEAD has one
    parent and no merge commit was introduced; the SUMMARY exists and names the
    `.continue-here.md` residue explicitly with a recommendation on whether #117 can close.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | This change edits three internal planning documents. No input parsing, no execution path, no published artifact, no consumer contract is touched. `spec-ci-plugin` reads `pattern_name` from this tool's JSON output; nothing here reaches that output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-sz4-01 | Tampering | `.planning/REQUIREMENTS.md` gate text | medium | mitigate | A weakened gate is a real governance risk: the amendment must preserve the one-category-per-unit substance, not relax it. Task 1's `<done>` pins the exact replacement text and Task 2's audit confirms no other gate row moved. |
| T-sz4-02 | Repudiation | GATE-04 amendment provenance | low | mitigate | The amended bullet carries its own date, issue number and the two counter-examples that justify it, so the reason the gate moved is readable from the requirement itself rather than only from a commit message. |
| T-sz4-03 | Information Disclosure | commit contents | low | accept | Docs-only, no secrets, no paths outside the repo. No package-manager install occurs in this plan, so the package-legitimacy gate does not apply. |
</threat_model>

<verification>
1. `bash -c '... VERIFY-OK'` from Task 1 passes — all three amended strings present, all three
   superseded forms absent, coverage counter unmoved.
2. `bash -c '... AUDIT-OK'` from Task 2 passes — exactly three files, all under `.planning/`,
   single-parent commit, summary written and naming the residue.
3. `git diff --name-only HEAD~1 HEAD` lists only `.planning/ROADMAP.md`, `.planning/STATE.md`,
   `.planning/REQUIREMENTS.md`.
4. Read the three amended regions side by side and confirm they say the same thing. This is the
   only check that actually closes #117; the greps prove the strings landed, not that the
   documents agree.
</verification>

<success_criteria>
- GATE-04 reads `No reviewable unit widens more than one category` in `.planning/ROADMAP.md`,
  `.planning/STATE.md` and `.planning/REQUIREMENTS.md`, in the developer's verbatim wording.
- `.planning/REQUIREMENTS.md` GATE-04 is `[x]`, its dated Phase 3 status paragraph replaced by the
  2026-09-16 amendment note citing #117, PR #120 and PR #136.
- The STATE.md / REQUIREMENTS.md contradiction named by issue #117 is gone.
- Zero changes outside the three named `.planning/` files.
- One non-merge commit; `main` still strictly linear.
- The fourth, out-of-scope copy in `.planning/.continue-here.md` is named in the summary rather
  than silently left behind.
</success_criteria>

<output>
Create `.planning/quick/260916-sz4-amend-gate-04-to-no-reviewable-unit-wide/260916-sz4-SUMMARY.md`
when done.
</output>
